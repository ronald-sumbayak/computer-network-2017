interpreter: python 3.6.1

port:
    3000

format input pada client:
    - ip : [pc public ip], "127.0.0.1", atau "localhost" (tanpa quotes)
    - msg: [number] [operator] [number] (masing-masing dipisahkan satu space)
           "exit" -> (tanpa quotes) terminate both client and server

-----------------
accepted operator:
    + (plus), - (minus), * (multiply), / (divide), ** (power), % (mod)


Ronald Andrean
5115100174